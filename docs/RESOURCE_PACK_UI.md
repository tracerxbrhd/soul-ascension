# SOUL ASCENSION UI resource pack contract

The purple SOUL ASCENSION interface is the only supported Character UI. A stale `legacy_ui` entry in an older client config is ignored by the current config specification.

See [`resourcepacks.md`](resourcepacks.md) for the stable texture paths and replacement rules.
