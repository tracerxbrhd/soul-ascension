# SOUL ASCENSION branding

The PNG files are the preferred publication/runtime sources. JPEG variants are retained as original distribution assets.

NeoForge uses the wide root resource `soul_ascension_banner.png` as the mod-list logo with nearest-neighbor filtering. The square icon remains available at `assets/soul_ascension/branding/icon.png`.
